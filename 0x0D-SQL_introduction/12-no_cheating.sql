-- Update the score of a record with name "Bob" to 10 in the table second_table
USE `hbtn_0c_0`;

-- Update the score for the record with name "Bob"
UPDATE `second_table` SET `score` = 10 WHERE `name` = 'Bob';
