-- Compute the average score of all records in the table second_table in the specified database
USE `hbtn_0c_0`;

-- Compute and display the average score
SELECT AVG(`score`) AS `average` FROM `second_table`;
